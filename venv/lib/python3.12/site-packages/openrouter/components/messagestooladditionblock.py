"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .anthropiccachecontroldirective import (
    AnthropicCacheControlDirective,
    AnthropicCacheControlDirectiveTypedDict,
)
from openrouter.types import BaseModel, UNSET_SENTINEL
from openrouter.utils import get_discriminator
from pydantic import Discriminator, Tag, model_serializer
from typing import Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


MessagesToolAdditionBlockTypeMcpToolsetReference = Literal["mcp_toolset_reference",]


class MessagesToolAdditionBlockToolMcpToolsetReferenceTypedDict(TypedDict):
    server_name: str
    type: MessagesToolAdditionBlockTypeMcpToolsetReference


class MessagesToolAdditionBlockToolMcpToolsetReference(BaseModel):
    server_name: str

    type: MessagesToolAdditionBlockTypeMcpToolsetReference


MessagesToolAdditionBlockTypeMcpToolReference = Literal["mcp_tool_reference",]


class MessagesToolAdditionBlockToolMcpToolReferenceTypedDict(TypedDict):
    name: str
    server_name: str
    type: MessagesToolAdditionBlockTypeMcpToolReference


class MessagesToolAdditionBlockToolMcpToolReference(BaseModel):
    name: str

    server_name: str

    type: MessagesToolAdditionBlockTypeMcpToolReference


MessagesToolAdditionBlockTypeToolReference = Literal["tool_reference",]


class MessagesToolAdditionBlockToolToolReferenceTypedDict(TypedDict):
    name: str
    type: MessagesToolAdditionBlockTypeToolReference


class MessagesToolAdditionBlockToolToolReference(BaseModel):
    name: str

    type: MessagesToolAdditionBlockTypeToolReference


MessagesToolAdditionBlockToolUnionTypedDict = TypeAliasType(
    "MessagesToolAdditionBlockToolUnionTypedDict",
    Union[
        MessagesToolAdditionBlockToolToolReferenceTypedDict,
        MessagesToolAdditionBlockToolMcpToolsetReferenceTypedDict,
        MessagesToolAdditionBlockToolMcpToolReferenceTypedDict,
    ],
)


MessagesToolAdditionBlockToolUnion = Annotated[
    Union[
        Annotated[MessagesToolAdditionBlockToolToolReference, Tag("tool_reference")],
        Annotated[
            MessagesToolAdditionBlockToolMcpToolReference, Tag("mcp_tool_reference")
        ],
        Annotated[
            MessagesToolAdditionBlockToolMcpToolsetReference,
            Tag("mcp_toolset_reference"),
        ],
    ],
    Discriminator(lambda m: get_discriminator(m, "type", "type")),
]


TypeToolAddition = Literal["tool_addition",]


class MessagesToolAdditionBlockTypedDict(TypedDict):
    r"""Loads a previously deferred tool (declared in `tools` with `defer_loading: true`) mid-conversation without invalidating the prompt cache. Only valid in `role: \"system\"` messages. Not supported on Claude Sonnet 5 or models older than Claude Opus 4.8."""

    tool: MessagesToolAdditionBlockToolUnionTypedDict
    type: TypeToolAddition
    cache_control: NotRequired[AnthropicCacheControlDirectiveTypedDict]
    r"""Enable automatic prompt caching. When set at the top level, the system automatically applies cache breakpoints to the last cacheable block in the request. When set on an individual content block, it marks an explicit cache breakpoint; block-level markers also work on OpenAI models that support explicit prompt caching — OpenRouter converts them to the provider's native format."""


class MessagesToolAdditionBlock(BaseModel):
    r"""Loads a previously deferred tool (declared in `tools` with `defer_loading: true`) mid-conversation without invalidating the prompt cache. Only valid in `role: \"system\"` messages. Not supported on Claude Sonnet 5 or models older than Claude Opus 4.8."""

    tool: MessagesToolAdditionBlockToolUnion

    type: TypeToolAddition

    cache_control: Optional[AnthropicCacheControlDirective] = None
    r"""Enable automatic prompt caching. When set at the top level, the system automatically applies cache breakpoints to the last cacheable block in the request. When set on an individual content block, it marks an explicit cache breakpoint; block-level markers also work on OpenAI models that support explicit prompt caching — OpenRouter converts them to the provider's native format."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["cache_control"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
