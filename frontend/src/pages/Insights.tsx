import { Box, Chip, Grid, Paper, Stack, Typography } from "@mui/material";
import { ArrowUpRight, Sparkles } from "lucide-react";
import { insightSignals } from "../models/intelligence";

export default function Insights() { return <Box sx={{ p: { xs: 3, md: 5 }, maxWidth: 1220, mx: "auto" }}><Typography sx={{ color: "#2563EB", fontSize: 11, letterSpacing: ".16em", fontWeight: 800, textTransform: "uppercase" }}>Intelligence / Financial Insights</Typography><Stack direction={{ xs: "column", sm: "row" }} sx={{ mt: 1, justifyContent: "space-between", alignItems: { sm: "end" } }}><Box><Typography sx={{ fontSize: { xs: 28, md: 34 }, fontWeight: 800, letterSpacing: "-.05em" }}>Signals worth acting on.</Typography><Typography sx={{ mt: .6, color: "#64748B" }}>Axora continuously interprets the movement behind your numbers.</Typography></Box><Chip icon={<Sparkles size={14} />} label="Updated 2 min ago" sx={{ mt: { xs: 2, sm: 0 }, backgroundColor: "#ECFDF5", color: "#059669", fontWeight: 700 }} /></Stack><Grid container spacing={2} sx={{ mt: 3 }}>{insightSignals.map((signal) => <Grid size={{ xs: 12, md: 4 }} key={signal.title}><Paper elevation={0} sx={{ height: "100%", p: 2.5, border: "1px solid #E2E8F0", borderRadius: 2.5 }}><Box sx={{ width: 38, height: 38, borderRadius: 1.2, backgroundColor: `${signal.color}18`, color: signal.color, display: "grid", placeItems: "center" }}><ArrowUpRight size={19} /></Box><Typography sx={{ mt: 2, fontWeight: 800, fontSize: 16, lineHeight: 1.35 }}>{signal.title}</Typography><Typography sx={{ mt: 1, color: "#64748B", fontSize: 13, lineHeight: 1.6 }}>{signal.description}</Typography><Stack direction="row" spacing={1} sx={{ mt: 2.5 }}><Chip size="small" label={`${signal.impact} impact`} sx={{ color: signal.color, backgroundColor: `${signal.color}12`, fontWeight: 700 }} /><Chip size="small" label={signal.owner} variant="outlined" sx={{ borderColor: "#E2E8F0", color: "#64748B" }} /></Stack></Paper></Grid>)}</Grid></Box>; }
