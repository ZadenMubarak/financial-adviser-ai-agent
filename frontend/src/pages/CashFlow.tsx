import { Box, Chip, Grid, Paper, Stack, Typography } from "@mui/material";
import { ArrowDownRight, ArrowUpRight, Landmark } from "lucide-react";
import { cashFlowAccounts, cashFlowEvents } from "../models/cashFlow";

export default function CashFlow() { return <Box sx={{ p: { xs: 3, md: 5 }, maxWidth: 1220, mx: "auto" }}><Typography sx={{ color: "#2563EB", fontSize: 11, letterSpacing: ".16em", fontWeight: 800, textTransform: "uppercase" }}>Finance / Cash Flow</Typography><Typography sx={{ mt: 1, fontSize: { xs: 28, md: 34 }, fontWeight: 800, letterSpacing: "-.05em" }}>Know your cash position.</Typography><Typography sx={{ mt: .6, color: "#64748B" }}>A live view of available capital and what is moving next.</Typography><Grid container spacing={2} sx={{ mt: 3 }}>{cashFlowAccounts.map((account) => <Grid size={{ xs: 12, md: 4 }} key={account.name}><Paper elevation={0} sx={{ p: 2.5, border: "1px solid #E2E8F0", borderRadius: 2.5 }}><Stack direction="row" sx={{ justifyContent: "space-between", alignItems: "start" }}><Box sx={{ width: 38, height: 38, borderRadius: 1.2, backgroundColor: `${account.color}18`, color: account.color, display: "grid", placeItems: "center" }}><Landmark size={18} /></Box><Chip size="small" label={account.change} sx={{ color: account.change.startsWith("+") ? "#059669" : "#D97706", backgroundColor: account.change.startsWith("+") ? "#ECFDF5" : "#FFFBEB", fontWeight: 700 }} /></Stack><Typography sx={{ mt: 2, fontWeight: 800 }}>{account.name}</Typography><Typography sx={{ mt: .4, color: "#94A3B8", fontSize: 12 }}>{account.institution}</Typography><Typography sx={{ mt: 1.4, fontSize: 25, fontWeight: 800, letterSpacing: "-.04em" }}>{account.balance}</Typography></Paper></Grid>)}</Grid><Paper elevation={0} sx={{ mt: 3, p: { xs: 2, md: 3 }, border: "1px solid #E2E8F0", borderRadius: 2.5 }}><Typography sx={{ fontWeight: 800, fontSize: 17 }}>Upcoming cash events</Typography><Typography sx={{ mt: .5, color: "#64748B", fontSize: 13 }}>Expected movement across connected accounts.</Typography><Stack sx={{ mt: 2 }}>{cashFlowEvents.map((event) => <Stack key={event.label} direction={{ xs: "column", sm: "row" }} spacing={1.5} sx={{ py: 1.6, borderBottom: "1px solid #F1F5F9", alignItems: { sm: "center" } }}><Typography sx={{ width: 60, color: "#64748B", fontSize: 12 }}>{event.date}</Typography><Box sx={{ flex: 1 }}><Typography sx={{ fontWeight: 700, fontSize: 14 }}>{event.label}</Typography><Typography sx={{ color: "#94A3B8", fontSize: 11 }}>{event.status}</Typography></Box><Chip size="small" icon={event.type === "Inflow" ? <ArrowUpRight size={13} /> : <ArrowDownRight size={13} />} label={event.type} sx={{ color: event.type === "Inflow" ? "#059669" : "#64748B", backgroundColor: event.type === "Inflow" ? "#ECFDF5" : "#F8FAFC", "& .MuiChip-icon": { color: "inherit" } }} /><Typography sx={{ minWidth: 90, textAlign: { sm: "right" }, fontWeight: 800, color: event.type === "Inflow" ? "#059669" : "#172333" }}>{event.amount}</Typography></Stack>)}</Stack></Paper></Box>; }
