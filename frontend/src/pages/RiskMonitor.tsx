import { Box, Chip, Grid, Paper, Stack, Typography } from "@mui/material";
import { AlertTriangle, ChevronRight, ShieldCheck } from "lucide-react";
import { riskItems } from "../models/risk";

export default function RiskMonitor() { return <Box sx={{ p: { xs: 3, md: 5 }, maxWidth: 1220, mx: "auto" }}><Typography sx={{ color: "#2563EB", fontSize: 11, letterSpacing: ".16em", fontWeight: 800, textTransform: "uppercase" }}>Risk / Risk Monitor</Typography><Stack direction={{ xs: "column", sm: "row" }} sx={{ mt: 1, justifyContent: "space-between", alignItems: { sm: "end" } }}><Box><Typography sx={{ fontSize: { xs: 28, md: 34 }, fontWeight: 800, letterSpacing: "-.05em" }}>Stay ahead of exposure.</Typography><Typography sx={{ mt: .6, color: "#64748B" }}>Unusual activity and emerging risk, ranked by impact.</Typography></Box><Box sx={{ display: "flex", alignItems: "center", gap: 1, mt: { xs: 2, sm: 0 }, color: "#059669" }}><ShieldCheck size={18} /><Typography sx={{ fontSize: 13, fontWeight: 700 }}>Systems healthy</Typography></Box></Stack><Grid container spacing={2} sx={{ mt: 3 }}>{riskItems.map((item) => <Grid size={{ xs: 12 }} key={item.title}><Paper elevation={0} sx={{ p: { xs: 2, md: 2.5 }, border: "1px solid #E2E8F0", borderRadius: 2.5 }}><Stack direction={{ xs: "column", md: "row" }} spacing={2} sx={{ alignItems: { md: "center" } }}><Box sx={{ width: 42, height: 42, flexShrink: 0, borderRadius: 1.3, backgroundColor: item.severity === "High" ? "#FEF2F2" : item.severity === "Medium" ? "#FFFBEB" : "#EFF6FF", color: item.severity === "High" ? "#DC2626" : item.severity === "Medium" ? "#D97706" : "#2563EB", display: "grid", placeItems: "center" }}><AlertTriangle size={20} /></Box><Box sx={{ flex: 1 }}><Typography sx={{ fontWeight: 800, fontSize: 15 }}>{item.title}</Typography><Typography sx={{ mt: .5, color: "#64748B", fontSize: 13 }}>{item.detail}</Typography></Box><Stack direction="row" spacing={1.5} sx={{ alignItems: "center" }}><Box sx={{ textAlign: "center", minWidth: 54 }}><Typography sx={{ fontSize: 21, fontWeight: 800 }}>{item.score}</Typography><Typography sx={{ color: "#94A3B8", fontSize: 10 }}>risk score</Typography></Box><Chip size="small" label={item.severity} sx={{ color: item.severity === "High" ? "#DC2626" : item.severity === "Medium" ? "#D97706" : "#2563EB", backgroundColor: item.severity === "High" ? "#FEF2F2" : item.severity === "Medium" ? "#FFFBEB" : "#EFF6FF", fontWeight: 700 }} /><ChevronRight size={17} color="#94A3B8" /></Stack></Stack><Stack direction="row" spacing={1} sx={{ mt: 1.5, pl: { md: 6.5 } }}><Typography sx={{ color: "#94A3B8", fontSize: 11 }}>{item.area}</Typography><Typography sx={{ color: "#CBD5E1", fontSize: 11 }}>·</Typography><Typography sx={{ color: "#94A3B8", fontSize: 11 }}>{item.time}</Typography></Stack></Paper></Grid>)}</Grid></Box>; }
